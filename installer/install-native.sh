#!/usr/bin/env bash
set -euo pipefail

# Placeholder installer for native Ubuntu targets (22.04/24.04).
# TODO: Implement:
#  - Prerequisite checks (root, Ubuntu version)
#  - Install Docker/Compose or native services as required
#  - Fetch tagged compose bundle from unison-platform
#  - Write /etc/unison/platform.env from user prompts or defaults
#  - Install and enable systemd units to start the stack on boot
echo "install-native.sh: placeholder script. Implementation pending."
