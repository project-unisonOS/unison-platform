# UnisonOS Platform Image — WSL2 (Developer)

**Who this release is for:** Developers on Windows using WSL2 for local development.

**Target platform:** Windows + WSL2

**Not for:** Linux VM users; bare-metal installs.

## Download

This release contains:

- `unisonos-wsl2-dev.tar.gz` — WSL2 distro import tarball (use `wsl --import`).

## Install

📘 Installation instructions: https://project-unisonos.github.io/unison-platform/install#wsl2

## Notes

- This repository distributes **platform images via GitHub Releases** (not GHCR).
- Versioned releases are immutable; see “latest per target” in `docs/release-strategy.md`.
